## 1. Backend Implementation
- [x] 1.1 Implement `POST /api/get` handler to execute `ghq get`.
- [x] 1.2 Implement `POST /api/create` handler to initialize a local repository.

## 2. UI Implementation
- [x] 2.1 Add "Add" button component near the search bar.
- [x] 2.2 Create a Modal/Dialog component for the input form.
- [x] 2.3 Implement the form with "Remote Repository Name" input and "Just create in local" checkbox.
- [x] 2.4 Wire up the "Clone" button to call `/api/get` or `/api/create` based on the checkbox state.